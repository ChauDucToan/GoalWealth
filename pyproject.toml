[project]
name = "goalwealth"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "selenium",
]

[tool.fastapi]
entrypoint = "goalwealth.main:app"